import { ImgLoadedDirective } from './img-loaded.directive';

describe('ImgLoadedDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgLoadedDirective();
    expect(directive).toBeTruthy();
  });
});
