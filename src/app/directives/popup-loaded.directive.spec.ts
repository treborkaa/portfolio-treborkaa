import { PopupLoadedDirective } from './popup-loaded.directive';

describe('PopupLoadedDirective', () => {
  it('should create an instance', () => {
    const directive = new PopupLoadedDirective();
    expect(directive).toBeTruthy();
  });
});
